/** @type {import('next').NextConfig} */
const nextConfig = {
  trailingSlash: true,
  basePath: "/population-chart",
  output: "export",
  reactStrictMode: true,
  // images: {
  //   unoptimized: true,
  // },
};

module.exports = nextConfig;
